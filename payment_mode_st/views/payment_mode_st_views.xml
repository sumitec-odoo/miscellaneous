<?xml version="1.0"?>
<odoo>

    <record id="payment_mode_st_view_form" model="ir.ui.view">
        <field name="name">payment.mode.st.form.view</field>
        <field name="model">payment.mode.st</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <field name="active" invisible="1" />
                    <group>
                        <group>
                            <field name="id" invisible="1"/>
                            <field name="name"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>    

    <record id="payment_mode_st_view_tree" model="ir.ui.view">
        <field name="name">payment.mode.st.list.view</field>
        <field name="model">payment.mode.st</field>
        <field name="arch" type="xml">
            <list>
                <field name="id" column_invisible="True"/>
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="payment_mode_st_action" model="ir.actions.act_window">
        <field name="name">Modos de pago</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">payment.mode.st</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_payment_mode_st"
        name="Modos de pago"
        parent="base.menu_users"
        action="payment_mode_st_action"
        sequence="100"/>

</odoo>
